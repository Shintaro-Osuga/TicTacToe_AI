public class AI{
    public static int[] eval(int[][] A, int depth)
    {
        int[] pos = {3,3};

        // Horizontal checker

        

        // Vertical checker

        // Diagonal checker
        return pos;
    }
}